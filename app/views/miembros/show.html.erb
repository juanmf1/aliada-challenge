<p style="color: green"><%= notice %></p>

  <div class="card bg-secondary m-2" style="width: 25rem; color: white" >
    <div class="card-header">
      Miembro
    </div>
    <div class="card-body d-flex flex-column">
      <div class="d-flex">
        <p><b>Nombre: </b></p>
        <p class="ms-1"><%= @miembro.nombre %></p>
      </div>
      <div class="d-flex">
        <p><b>Apellido: </b></p>
        <p class="ms-1"><%= @miembro.apellido %></p>
      </div>
    </div>
    <div class="card-footer d-flex">
      <%= link_to "Editar", edit_ticket_path(@miembro), :class => "btn btn-warning me-2" %>
      <%= button_to "Eliminar", @miembro, method: :delete, :class => "btn btn-danger" %>
    </div>
  </div>

